# hostinger_dbo

A lightweight PHP database object framework designed for Hostinger shared hosting. It provides object-style database access and a RESTful API layer over your MySQL/MariaDB database.

This project lays the groundwork for building applications that interact with a database through a unified object layer or via REST endpoints. See the `docs` directory for architecture and API design details.
