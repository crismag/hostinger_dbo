# Signed project insert

Use the signing steps in [select.md](select.md) with path `/api/v1/projects/insert` and this JSON body:

```json
{"data":{"tenant_id":"tenant_001","name":"Example Project","status":"active"}}
```
